LoadLayers-QGISplugin
=====================

Plugin to make it easy for users to load predefined layers by an easy selection dialog.

Plugin is written based on the "QGIS Plugin Builder" template, using Python.

It uses some variables to config the tool and create (if not existing) a list of layers that should be included in the tool.

The layers are consistent with the QgsVectorLayer and QgsRasterLayer commands and in the layer list file examples of supported layers are included, just edit to your likeing.

QGIS insticksprogram - Ladda Lager
==================================

Insticksprogrammet hämtar fördefinierade lager från en listfil och gör det enkelt att lägga till dessa från en dialogruta i QGIS.

Tillägget läser innehåll från en utpekad fil. Programmet stödjer det vektor- och rasterlagerkommando som finns i QGIS och exempel på många av dessa lager visas i den inkluderade listfilen (lagerlista.txt). Med dessa som guide så skall det förhoppningsvis vara ganska enkelt att göra en egen lista.

